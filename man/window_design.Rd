% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window_design.R
\name{window_design}
\alias{window_design}
\title{Translate tile design into WindowedProblem and BatchProblem parameters}
\usage{
window_design(
  r,
  design = NULL,
  tile_d = NULL,
  tile_trim = NULL,
  landmark = NULL,
  centersize = NULL,
  buffer = NULL,
  window_units = c("cells", "map")
)
}
\arguments{
\item{r}{\code{SpatRaster} used to determine cell size, raster dimensions, and
approximate job counts. This should be the analysis raster or a raster
with the same resolution and extent.}

\item{design}{Optional object returned by \code{\link[=tile_design]{tile_design()}}. If supplied,
\code{tile_d}, \code{tile_trim}, \code{landmark}, \code{theta}, and \code{distance_scale} are read
from this object unless explicitly overridden.}

\item{tile_d}{Interior tile or center-window width in map units. Optional
when \code{design} is supplied or when \code{centersize} and \code{buffer} are supplied.}

\item{tile_trim}{Buffer or trim distance in map units. Optional when
\code{design} is supplied or when \code{centersize} and \code{buffer} are supplied.}

\item{landmark}{Integer coarse-graining window size used for landmark
alignment. If \code{NULL}, the value is taken from \code{design} when available,
otherwise \code{10L}.}

\item{centersize}{Optional center window size. If supplied, \code{buffer} must
also be supplied. By default this is interpreted in cells.}

\item{buffer}{Optional buffer width around each center window. If supplied,
\code{centersize} must also be supplied. By default this is interpreted in
cells.}

\item{window_units}{Units for explicitly supplied \code{centersize} and \code{buffer}:
\code{"cells"} or \code{"map"}.}
}
\value{
A named list of class \code{"ConScapeRtools_window_design"} with:
\itemize{
\item \code{centersize} and \code{buffer} in cells for \code{run_conscape()}.
\item \code{tile_d} and \code{tile_trim} in map units for \code{\link[=conscape_prep]{conscape_prep()}}.
\item \code{run_args}, a small list suitable for \code{run_conscape(..., backend = "conscape_dev")}.
\item \code{prep_args}, a small list suitable for \code{\link[=conscape_prep]{conscape_prep()}} center-buffer
preparation.
\item \code{diagnostics}, including expected window count, source/context window
cells, center cells, overlap area factor, and a workload proxy.
}
}
\description{
Converts a map-unit ConScapeRtools tile design into the cell-unit
\code{centersize} and \code{buffer} parameters used by the experimental
\code{backend = "conscape_dev"} workflows. Use \code{\link[=tile_design]{tile_design()}} first when the
goal is to choose biologically meaningful distances from a species'
dispersal ability. Use \code{window_design()} after that to make the same design
usable by ConScape development \code{WindowedProblem} and \code{BatchProblem} objects.
}
\details{
ConScape development windows separate two ideas that are combined in the
classic tiled workflow:
\itemize{
\item \code{centersize} is the retained target window. These are the cells whose
outputs are intended to be kept from a given window.
\item \code{buffer} is the contextual margin around the center. Source qualities,
target qualities, and affinities in this margin are used in the
calculation so the center cells see nearby landscape context.
}

The total source/context window has width \code{centersize + 2 * buffer} cells.
For randomized shortest-path connectivity, a useful first-order workload
proxy is:

\code{source_window_cells * center_cells * expected_windows}.

This is not an exact runtime formula, but it tracks the key scaling pressure:
more source cells and more target cells make each window more expensive, and
smaller center windows increase the number of windows.

When \code{design} or map-unit \code{tile_d} and \code{tile_trim} are supplied,
\code{window_design()} calls the same internal landmark-alignment logic used by
\code{\link[=conscape_prep]{conscape_prep()}}. This makes the returned \code{centersize} and \code{buffer}
consistent with the classic tiled workflow:
\itemize{
\item \code{centersize = tile_cells}
\item \code{buffer = overlap_cells}
\item \verb{tile_d = centersize * cell size}
\item \verb{tile_trim = buffer * cell size}
}

Practical guidance:
\itemize{
\item Choose \code{buffer} from biology first. In practice this means using
\code{\link[=tile_design]{tile_design()}} with a defensible \code{max_d} and \code{trim_threshold}.
\item Choose \code{centersize} from compute constraints second. Larger centers
reduce repeated overlap work but increase per-window memory and solve
size. Smaller centers are safer for memory but repeat more buffered
context.
\item Inspect \code{overlap_area_factor}. Values near 2 to 4 are often reasonable
starting points. Very large values mean the buffer dominates the center,
so most compute is repeated context.
\item Use \code{dev_mode = "windowed"} first to test correctness and API behavior.
Use \code{dev_mode = "batch"} when the same design should be scheduled as
resumable on-disk jobs.
}
}
\examples{
library(ConScapeRtools)
r <- terra::rast(system.file("extdata", "suitability.asc",
                            package = "ConScapeRtools"))

wd <- window_design(
  r = r,
  tile_d = 5400,
  tile_trim = 2700,
  landmark = 10L
)

wd$run_args
wd$diagnostics

}
\seealso{
\code{\link[=tile_design]{tile_design()}}, \code{\link[=conscape_prep]{conscape_prep()}}, \code{\link[=run_conscape]{run_conscape()}}
}
