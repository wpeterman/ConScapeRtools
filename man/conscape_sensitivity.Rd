% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conscape_api.R
\name{conscape_sensitivity}
\alias{conscape_sensitivity}
\title{Configure ConScape sensitivity outputs}
\usage{
conscape_sensitivity(
  wrt = c("Q", "A&C=f(A)"),
  landscape_measure = c("sum", "eigenanalysis"),
  unitless = TRUE,
  method = c("analytical", "simulation"),
  one_out_of = 1L,
  diagvalue = NULL,
  target_equal_source = TRUE,
  require_landmark_one = TRUE
)
}
\arguments{
\item{wrt}{Character vector describing the local perturbation target.
Supported values are \code{"Q"} (habitat quality), \code{"A"} (affinity),
\code{"C"} (cost), \code{"A&C=f(A)"} (linked affinity-to-cost change), and
\code{"C&A=f(C)"} (linked cost-to-affinity change).}

\item{landscape_measure}{Summary of the landscape matrix: \code{"sum"} or
\code{"eigenanalysis"}.}

\item{unitless}{Logical. If \code{TRUE}, return proportional sensitivities
(elasticities). If \code{FALSE}, return raw unit-change sensitivities.}

\item{method}{\code{"analytical"} uses \code{ConScape.sensitivity}; \code{"simulation"}
uses \code{ConScape.sensitivity_simulation}.}

\item{one_out_of}{Integer subsampling interval used only for simulation.}

\item{diagvalue}{Optional diagonal value passed to ConScape. Use \code{NULL} for
ConScape's default.}

\item{target_equal_source}{Logical passed to ConScape. The current
sensitivity implementation expects target quality to equal source quality.}

\item{require_landmark_one}{Logical. If \code{TRUE} (default),
\code{\link[=run_conscape]{run_conscape()}} requires \code{landmark = 1L} for sensitivity runs so that
ConScape's target qualities are not changed by coarse graining.}
}
\value{
An object of class \code{"ConScapeSensitivitySpec"}.
}
\description{
Creates a sensitivity specification for \code{\link[=run_conscape]{run_conscape()}}. Analytical
sensitivity is available when the installed Julia ConScape package provides
\code{ConScape.sensitivity}, currently on the ConScape \code{sensitivity} branch or a
future release containing that function. Install that build once per machine
with \code{\link[=conscape_sensitivity_setup]{conscape_sensitivity_setup()}}; the default registered ConScape package
does not include these functions.
}
\details{
Sensitivity analysis asks how a landscape-level connectivity summary changes
when a local component of the landscape is perturbed. The \code{wrt} argument
controls which component is perturbed:
\itemize{
\item \code{"Q"} evaluates sensitivity to habitat quality.
\item \code{"A"} evaluates sensitivity to affinities/permeability.
\item \code{"C"} evaluates sensitivity to movement costs.
\item \code{"A&C=f(A)"} evaluates the linked case where changing affinity also changes
cost through the chosen cost transformation.
\item \code{"C&A=f(C)"} evaluates the reverse linked case.
}

The default \code{unitless = TRUE} returns elasticities: sensitivities scaled by
the value of the perturbed quantity. These are usually easier to compare
across habitat-quality and movement surfaces because they describe response
to proportional change. Set \code{unitless = FALSE} for raw unit-change
sensitivities.

For tiled workflows, sensitivity is computed independently within each tile
and then mosaicked by \code{\link[=run_conscape]{run_conscape()}}. Two preconditions apply:
\enumerate{
\item The \code{\link[=conscape_prep]{conscape_prep()}} object must be built with
\code{target_mode = "full"}. The new default \code{target_mode = "center"}
(which fcon / btwn rely on) sets per-tile target qualities to a
strict subset of source qualities; ConScape's sensitivity API
assumes target equals source per cell, which is violated inside
every center-mode tile. \code{\link[=run_conscape]{run_conscape()}} refuses the combination
and tells you to recreate the prep with \code{target_mode = "full"}.
\item Build the prep with \code{landmark = 1L} so that coarse graining does
not alter target qualities.
}

Even with both preconditions satisfied, tiled sensitivity is a
tile-local approximation to landscape-level sensitivity. Each tile
computes the derivative of its own per-tile landscape summary, not
the global one, and \code{\link[=run_conscape]{run_conscape()}} mean-mosaics overlapping
per-tile estimates. The approximation converges to the untiled
answer as buffer grows; see
\code{tests/testthat/test-sensitivity-convergence.R} for a reproducible
convergence sweep. Use \code{landscape_measure = "sum"} for the most
defensible tiled estimator. \code{landscape_measure = "eigenanalysis"}
is available through ConScape but should be interpreted cautiously
because eigenvalue summaries are intrinsically global. For strict
correctness, run sensitivity untiled when the landscape fits in a
single graph.
}
\examples{
sens <- conscape_sensitivity(wrt = c("Q", "A&C=f(A)"))
sens$wrt
sens$unitless

raw_sens <- conscape_sensitivity(wrt = "A", unitless = FALSE)
raw_sens$unitless
}
