% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efficiency_assessment.R
\name{conscape_efficiency_assessment}
\alias{conscape_efficiency_assessment}
\title{Assess ConScape prep workload}
\usage{
conscape_efficiency_assessment(..., reference = 1L)
}
\arguments{
\item{...}{One or more \code{"ConScapeRtools_prep"} objects. Named arguments are
used as scenario labels. Unnamed inputs receive labels such as \code{"prep_1"}
and \code{"prep_2"} in the order supplied.}

\item{reference}{Integer or character identifying the reference scenario for
reduction columns. Defaults to the first supplied prep object.}
}
\value{
A data frame with one row per prep object. Reduction columns are proportions
relative to \code{reference}, so \code{0.25} means a 25 percent reduction.
}
\description{
Summarize tile-level workload proxies from one or more objects created by
\code{\link[=conscape_prep]{conscape_prep()}}. This is useful for comparing tile designs before
launching a costly Julia run.
}
\details{
ConScape randomized shortest path calculations scale strongly with the
number of usable source graph cells and target quality cells in each tile.
The dominant cost per tile is solving \verb{Z = (I - W) \\ I_target} where the
number of columns of \code{Z} equals the number of positive target cells. Two
workload proxies are reported:
\itemize{
\item \code{estimated_source_target_cells} summarizes the work the ConScape backend
actually performs per prep object. For \code{target_mode = "center"} (the new
default), each tile's backend target raster is restricted to its center
cells, so this column uses \code{n_positive_move * n_positive_output_target}.
For \code{target_mode = "full"} (the legacy mode), the full buffered target
raster is solved, so this column uses \code{n_positive_move * n_positive_target}.
\item \code{estimated_output_source_target_cells} always uses
\code{n_positive_move * n_positive_output_target}. It reflects the work that
\emph{would} be required if center-target windowing were used, regardless of
the actual target mode of the prep.
}

Likewise, \code{positive_target_cells} reports the count of positive cells in the
buffered tile's target raster (pre-restriction), and
\code{positive_output_target_cells} reports the count of positive target cells
that are written to disk and consumed by the backend. For
\code{target_mode = "center"}, the two differ; for \code{target_mode = "full"}, they
are equal.
}
\examples{
r <- terra::rast(nrows = 4, ncols = 4, xmin = 0, xmax = 4,
                 ymin = 0, ymax = 4)
terra::values(r) <- 1
prep_a <- conscape_prep(
  tile_d = 2, tile_trim = 0,
  asc_dir = tempfile("prep-a-"),
  r_target = r, r_mov = r, r_src = r,
  clear_dir = TRUE, landmark = 1L, progress = FALSE
)
prep_b <- conscape_prep(
  centersize = 2, buffer = 1,
  asc_dir = tempfile("prep-b-"),
  r_target = r, r_mov = r, r_src = r,
  clear_dir = TRUE, landmark = 1L, progress = FALSE
)
conscape_efficiency_assessment(classic = prep_a, centered = prep_b)

\dontrun{
classic <- conscape_prep(
  tile_d = 5000,
  tile_trim = 1000,
  r_target = habitat,
  r_mov = affinity,
  r_src = habitat,
  clear_dir = TRUE
)

centered <- conscape_prep(
  centersize = 100,
  buffer = 20,
  r_target = habitat,
  r_mov = affinity,
  r_src = habitat,
  clear_dir = TRUE
)

conscape_efficiency_assessment(classic = classic, centered = centered)
}
}
